<!-- 678d95db-fca7-4969-ace1-d0a7f662109e 9a56cd74-07d4-43bb-9c29-b518220c3d49 -->
# Background Model Integration Plan

1. Update model configuration (`Cloutmate/Models/ModelTierMap.swift`) to register the new `smollm2:1.7b` identifier for internal/background use without altering the existing Gwen/Granite routing.
2. Extend `OllamaBridgeService` with a background-model helper: define a `backgroundModel` constant, optionally pre-warm it, and add a utility that wraps `makeOllamaRequest` using `smollm2:1.7b`.
3. Refactor background inference methods in `OllamaBridgeService.swift` (intent detection, preference inference, summaries, tagging, insights, etc.) to call the new helper so only Gwen/Granite handle conversational replies.
4. Tune Aurora’s tone/personality instructions (`AuroraSystemPromptBuilder.swift`, related services) so foreground replies stay ChatGPT-like—casual empathy by default, smooth transition into structured productivity help when the user pivots—without losing existing productivity intelligence cues.
5. Refresh inline comments or lightweight docs (if needed) to clarify that smolLM2 now powers silent context enrichment while Gwen/Granite remain the visible assistants.