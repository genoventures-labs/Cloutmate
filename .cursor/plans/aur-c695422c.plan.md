<!-- c695422c-9ab0-47f3-80c5-f081df1b0934 43e69d65-8345-4d4c-a9e9-f84d4e03f593 -->
# Casual Tone Alignment Plan

1. propagate-casual-flag — Extend `ModelRoutingEngine.selectModel` to surface the casual/conversation flag, update `OllamaBridgeService` to reuse it, and feed a relaxed directive into `AuroraSystemPromptBuilder` (skip productivity nudges when `CasualConversationDetector` fires).
2. tune-style-weighting — Adjust `StyleAdapter.instructions` weighting so recent low-formality inputs (e.g., "wanna hear something funny") override stale preferences faster; include safety checks for extremes.
3. enrich-style-signals — Update `StyleAnalyzer` to recognize textual emojis (`XD`, `:p`, `lol`) and boost casual markers, improving energy/emoji scores for playful messages.
4. soften-context-defaults — Revise `ContextualAdaptationService.generateContextualInstructions` defaults so we only inject workload-support cues when real data signals burden; add a casual override hook used in step 1.

Todos:

- propagate-casual-flag: Surface casual mode through routing and prompt builder
- tune-style-weighting: Rebalance persistent/current weighting in `StyleAdapter`
- enrich-style-signals: Expand `StyleAnalyzer` casual detectors
- soften-context-defaults: Make workload cues opt-in and support casual override