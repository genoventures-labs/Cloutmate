<!-- a585e395-1c40-4a40-b8f0-05bdcf33c2c4 86855a4b-8d3d-4a9a-b41c-9e81cfab5f17 -->
# Aurora Chat Panel V3

## Layout + Container

- update `UnifiedAIAssistantView.swift` to mount a floating `AuroraChatPanel` anchored bottom-right, supply environment bindings, and introduce collapse/orb transitions
- add `AuroraChatContainer.swift` to host the threaded scroll view, message bubbles, and theme-driven background blur with animated response/pulse hooks
- add `CollapsedChatView.swift` for the idle orb state and integrate breathing animation reuse

## Toolbar + Input System

- create modular toolbar components `AttachmentMenuView.swift`, `ActionMenuView.swift`, `VoiceButton.swift`, `WebButton.swift` and compose them inside the chat panel input row
- replace the legacy composer usage with a new inline input field that stretches on Shift+Enter, rotates placeholders, and surfaces context chips with hover/detach affordances

## Animation, Context, Accessibility

- add `AuroraOrbView.swift` (or reuse existing implementation) for pulse/breath cycles tied to typing, reply, and collapse animations
- extend `AIAssistantViewModel.swift` (and supporting theme utilities) for context-tag detection, dynamic placeholder rotation, collapsed-state state handling, and keyboard shortcuts (cmd bindings)
- ensure VoiceOver labels, hover cues, and theme-aware glow adjustments through `GlassColorSystem` / `ThemeManager`

### To-dos

- [ ] Implement floating chat panel, message container, and collapsed orb integration
- [ ] Build new toolbar/input components and wire them into the panel
- [ ] Hook animations, context metadata, and accessibility/shortcut updates in the view model